apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.guru.demo', name: 'dropwizard-core', version: '1.0.0'
}

apply plugin: 'application'

mainClassName = 'com.guru.rest.App'

run {
    args 'server'
}